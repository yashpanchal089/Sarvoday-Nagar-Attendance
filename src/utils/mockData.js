// Clean starting seed lists for Sarvoday Yuvak Mandal
export const initialUsers = [];

export const initialActivities = [];

export const generateMockAttendanceHistory = (users) => {
  return {};
};
